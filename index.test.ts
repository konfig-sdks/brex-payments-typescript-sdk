import { BrexPayments } from "./index";

describe("brex-payments-typescript-sdk", () => {
    it("initialize client", async () => {
        const brexpayments = new BrexPayments({
        });
    });
});
